#include "main.h"

/**
 * _isupper - returns whether a character's ASCII value is uppercase
 * @c: ASCII character to be checked
 * Return: 1 if it is uppercase and 0 for any else
 */
int _isupper(int c)
{
  if (x >= 65 && x <= 90)
  {
  return (1);
  }
  return (0);
}
